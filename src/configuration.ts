// Hardcoded Gateway ID as we only anticipate one Stripe account
// We can move this to a configuration option if that changes
export const gatewayId: string = 'xyz';

export const stripeStyles: any = {
    base: {
        fontSize: '24px',
    },
};
